{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main Issue Note window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-default-urls"
  ]
}
